"""Coworkers and their shifts: AI workers that run like clockwork (docs/SCOPE_SHIFTS.md).

`contract` is the public part, the file a partner writes and the receipt every run ends with.
It is versioned and is never changed incompatibly (docs/COWORKER_CONTRACT.md).
"""
